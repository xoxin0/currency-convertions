[pytest]
# Тесты лежат только в каталоге tests/.
# Это не даёт pytest подхватывать scripts/performance_test.py
# (имя подходит под маску *_test.py), который является самостоятельным
# нагрузочным скриптом и требует запущенного сервера.
testpaths = tests
